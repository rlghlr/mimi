rules_version = '2';
// All data access goes through the server (Admin SDK), which bypasses these
// rules. Direct client access is denied — clients never read/write Firestore.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
